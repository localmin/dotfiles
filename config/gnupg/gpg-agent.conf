# Ask for passphrases through the macOS dialog rather than a curses prompt in
# the terminal. gpg-agent delegates the prompt to this program, so the
# passphrase never passes through gpg itself or the calling program.
pinentry-program /opt/homebrew/bin/pinentry-mac
